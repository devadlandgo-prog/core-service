{
  "databaseChangeLog": [
    {
      "changeSet": {
        "id": "0-create-schema",
        "author": "landgo",
        "changes": [
          {
            "sql": {
              "sql": "CREATE SCHEMA IF NOT EXISTS core;"
            }
          }
        ],
        "runOnChange": true,
        "failOnError": false
      }
    },
    {
      "changeSet": {
        "id": "1-core-service-schema",
        "author": "landgo",
        "changes": [
          {
            "sql": {
              "sql": "SET search_path TO core; CREATE TABLE IF NOT EXISTS users (id UUID PRIMARY KEY, email VARCHAR(255) NOT NULL UNIQUE, full_name VARCHAR(255), profile_image_url VARCHAR(500), deleted BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);"
            }
          },
          {
            "sql": {
              "sql": "SET search_path TO core; CREATE TABLE IF NOT EXISTS vendor_profiles (id UUID PRIMARY KEY, user_id UUID NOT NULL UNIQUE REFERENCES users(id), company_name VARCHAR(100) NOT NULL, company_description TEXT, company_logo VARCHAR(500), business_license VARCHAR(255), business_address VARCHAR(255) NOT NULL, business_city VARCHAR(100) NOT NULL, business_state VARCHAR(100) NOT NULL, business_zip_code VARCHAR(20) NOT NULL, business_country VARCHAR(100) NOT NULL, website VARCHAR(255), verified BOOLEAN NOT NULL DEFAULT FALSE, rating DECIMAL(3,2), total_reviews INTEGER DEFAULT 0, total_lands_listed INTEGER DEFAULT 0, total_lands_sold INTEGER DEFAULT 0, deleted BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP); CREATE INDEX IF NOT EXISTS idx_vendor_user ON vendor_profiles(user_id);"
            }
          },
          {
            "sql": {
              "sql": "SET search_path TO core; CREATE TABLE IF NOT EXISTS lands (id UUID PRIMARY KEY, vendor_id UUID NOT NULL, project_stage VARCHAR(50) NOT NULL, status VARCHAR(50) NOT NULL DEFAULT 'PENDING_APPROVAL', address VARCHAR(255) NOT NULL, city VARCHAR(100) NOT NULL, postal_code VARCHAR(20) NOT NULL, lot_size DECIMAL(15,2) NOT NULL, lot_unit VARCHAR(20) NOT NULL, frontage VARCHAR(50), depth VARCHAR(50), current_zoning_codes VARCHAR(255), pin_number VARCHAR(50), official_plan_designation VARCHAR(255), latitude DECIMAL(10,8), longitude DECIMAL(11,8), project_specification JSONB, asking_price DECIMAL(15,2) NOT NULL, currency VARCHAR(10) NOT NULL DEFAULT 'CAD', pricing_description TEXT, photos JSONB, documents JSONB, ownership_verification VARCHAR(500), view_count INTEGER DEFAULT 0, inquiry_count INTEGER DEFAULT 0, deleted BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP); CREATE INDEX IF NOT EXISTS idx_land_vendor ON lands(vendor_id); CREATE INDEX IF NOT EXISTS idx_land_status ON lands(status); CREATE INDEX IF NOT EXISTS idx_land_city ON lands(city);"
            }
          },
          {
            "sql": {
              "sql": "SET search_path TO core; CREATE TABLE IF  NOT EXISTS listing_drafts (id UUID PRIMARY KEY, owner_id UUID NOT NULL REFERENCES users(id), status VARCHAR(20) NOT NULL DEFAULT 'IN_PROGRESS', current_step INTEGER DEFAULT 0, step1_data JSONB, step2_data JSONB, step3_data JSONB, step4_data JSONB, step5_data JSONB, published_listing_id UUID REFERENCES lands(id), deleted BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);"
            }
          },
          {
            "sql": {
              "sql": "SET search_path TO core; CREATE TABLE IF NOT EXISTS reviews (id UUID PRIMARY KEY, professional_id UUID NOT NULL, author_id UUID NOT NULL REFERENCES users(id), rating INTEGER NOT NULL, title VARCHAR(200) NOT NULL, content TEXT NOT NULL, deleted BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, UNIQUE(author_id, professional_id)); CREATE INDEX IF NOT EXISTS idx_reviews_professional_id ON reviews(professional_id);"
            }
          },
          {
            "sql": {
              "sql": "SET search_path TO core; CREATE TABLE IF NOT EXISTS saved_searches (id UUID PRIMARY KEY, user_id UUID NOT NULL REFERENCES users(id), name VARCHAR(100) NOT NULL, keyword VARCHAR(200), city VARCHAR(100), project_stage VARCHAR(30), min_price DECIMAL(15,2), max_price DECIMAL(15,2), min_lot_size DECIMAL(15,2), max_lot_size DECIMAL(15,2), notifications_enabled BOOLEAN DEFAULT TRUE, match_count INTEGER DEFAULT 0, last_notified_at TIMESTAMP, deleted BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);"
            }
          }
        ]
      }
    },
    {
      "changeSet": {
        "id": "2-add-favorites-and-inquiries",
        "author": "landgo",
        "changes": [
          {
            "sql": {
              "sql": "SET search_path TO core; CREATE TABLE IF NOT EXISTS favorite_listings (id UUID PRIMARY KEY, user_id UUID NOT NULL REFERENCES users(id), land_id UUID NOT NULL REFERENCES lands(id) ON DELETE CASCADE, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, UNIQUE(user_id, land_id)); CREATE INDEX IF NOT EXISTS idx_fav_user ON favorite_listings(user_id);"
            }
          },
          {
            "sql": {
              "sql": "SET search_path TO core; CREATE TABLE IF NOT EXISTS inquiries (id UUID PRIMARY KEY, listing_id UUID REFERENCES lands(id) ON DELETE CASCADE, professional_id UUID REFERENCES vendor_profiles(user_id) ON DELETE CASCADE, sender_name VARCHAR(255) NOT NULL, sender_email VARCHAR(255) NOT NULL, message TEXT NOT NULL, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP); CREATE INDEX IF NOT EXISTS idx_inq_listing ON inquiries(listing_id); CREATE INDEX IF NOT EXISTS idx_inq_prof ON inquiries(professional_id);"
            }
          },
          {
            "sql": {
              "sql": "SET search_path TO core; ALTER TABLE lands ADD COLUMN IF NOT EXISTS is_featured BOOLEAN NOT NULL DEFAULT FALSE, ADD COLUMN IF NOT EXISTS is_developer_deal BOOLEAN NOT NULL DEFAULT FALSE;"
            }
          }
        ]
      }
    },
    {
      "changeSet": {
        "id": "3-add-expertise-options",
        "author": "landgo",
        "changes": [
          {
            "sql": {
              "sql": "SET search_path TO core; CREATE TABLE IF NOT EXISTS expertise_options (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), name VARCHAR(100) NOT NULL UNIQUE, active BOOLEAN NOT NULL DEFAULT TRUE, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP); INSERT INTO expertise_options (name) VALUES ('Land Surveying'), ('Architecture'), ('Legal Advice'), ('Civil Engineering'), ('Environmental Assessment'), ('Urban Planning'), ('Real Estate Law'), ('Property Appraisal') ON CONFLICT (name) DO NOTHING;"
            }
          }
        ]
      }
    },
    {
      "changeSet": {
        "id": "4-add-inquiry-status-and-verification-status",
        "author": "landgo",
        "changes": [
          {
            "sql": {
              "sql": "SET search_path TO core; ALTER TABLE inquiries ADD COLUMN IF NOT EXISTS status VARCHAR(20) NOT NULL DEFAULT 'PENDING'; ALTER TABLE vendor_profiles ADD COLUMN IF NOT EXISTS verification_status VARCHAR(20) NOT NULL DEFAULT 'PENDING'; UPDATE vendor_profiles SET verification_status = 'APPROVED' WHERE verified = TRUE; UPDATE vendor_profiles SET verification_status = 'PENDING' WHERE verified = FALSE;"
            }
          }
        ]
      }
    },
    {
      "changeSet": {
        "id": "5-align-with-latest-entities",
        "author": "landgo",
        "changes": [
          {
            "sql": {
              "sql": "SET search_path TO core; ALTER TABLE vendor_profiles ADD COLUMN IF NOT EXISTS license_number VARCHAR(50), ADD COLUMN IF NOT EXISTS years_of_experience INTEGER, ADD COLUMN IF NOT EXISTS phone_number VARCHAR(20), ADD COLUMN IF NOT EXISTS bio TEXT, ADD COLUMN IF NOT EXISTS verification_notes VARCHAR(500); ALTER TABLE reviews ADD COLUMN IF NOT EXISTS verified_purchase BOOLEAN NOT NULL DEFAULT FALSE;"
            }
          },
          {
            "sql": {
              "sql": "SET search_path TO core; CREATE TABLE IF NOT EXISTS vendor_specializations (vendor_id UUID NOT NULL REFERENCES vendor_profiles(id) ON DELETE CASCADE, specialization VARCHAR(255)); CREATE TABLE IF NOT EXISTS vendor_service_areas (vendor_id UUID NOT NULL REFERENCES vendor_profiles(id) ON DELETE CASCADE, service_area VARCHAR(255)); CREATE TABLE IF NOT EXISTS vendor_certifications (vendor_id UUID NOT NULL REFERENCES vendor_profiles(id) ON DELETE CASCADE, certification VARCHAR(255)); CREATE TABLE IF NOT EXISTS review_tags (review_id UUID NOT NULL REFERENCES reviews(id) ON DELETE CASCADE, tag VARCHAR(255)); CREATE TABLE IF NOT EXISTS review_photos (review_id UUID NOT NULL REFERENCES reviews(id) ON DELETE CASCADE, photo_url VARCHAR(2000));"
            }
          }
        ]
      }
    },
    {
      "changeSet": {
        "id": "6-add-featured-and-hot-deal-to-lands",
        "author": "landgo",
        "changes": [
          {
            "sql": {
              "sql": "SET search_path TO core; ALTER TABLE lands ADD COLUMN IF NOT EXISTS is_featured BOOLEAN NOT NULL DEFAULT FALSE, ADD COLUMN IF NOT EXISTS is_hot_deal BOOLEAN NOT NULL DEFAULT FALSE;"
            }
          }
        ]
      }
    },
    {
      "changeSet": {
        "id": "7-create-expertise-and-pro-enquiries",
        "author": "landgo",
        "changes": [
          {
            "sql": {
              "sql": "SET search_path TO core; CREATE TABLE IF NOT EXISTS expertise_options (id UUID PRIMARY KEY, name VARCHAR(100) NOT NULL UNIQUE, is_active BOOLEAN DEFAULT TRUE, created_at TIMESTAMP NOT NULL, updated_at TIMESTAMP NOT NULL); CREATE TABLE IF NOT EXISTS professional_enquiries (id UUID PRIMARY KEY, vendor_id UUID NOT NULL, sender_id UUID, sender_name VARCHAR(255) NOT NULL, sender_email VARCHAR(255) NOT NULL, message TEXT NOT NULL, status VARCHAR(50) NOT NULL, created_at TIMESTAMP NOT NULL, updated_at TIMESTAMP NOT NULL);"
            }
          },
          {
            "sql": {
              "sql": "SET search_path TO core; INSERT INTO expertise_options (id, name, is_active, created_at, updated_at) VALUES (gen_random_uuid(), 'Land Surveying', true, now(), now()), (gen_random_uuid(), 'Architecture', true, now(), now()), (gen_random_uuid(), 'Legal Advice', true, now(), now()), (gen_random_uuid(), 'Civil Engineering', true, now(), now()), (gen_random_uuid(), 'Environmental Assessment', true, now(), now()), (gen_random_uuid(), 'Urban Planning', true, now(), now()), (gen_random_uuid(), 'Real Estate Law', true, now(), now()), (gen_random_uuid(), 'Property Appraisal', true, now(), now()) ON CONFLICT (name) DO NOTHING;"
            }
          }
        ]
      }
    },
    {
      "changeSet": {
        "id": "8-create-locations-table",
        "author": "landgo",
        "changes": [
          {
            "sql": {
              "sql": "SET search_path TO core; CREATE TABLE IF NOT EXISTS locations (id UUID PRIMARY KEY, name VARCHAR(255) NOT NULL, code VARCHAR(10) NOT NULL, type VARCHAR(20) NOT NULL, parent_id UUID, is_active BOOLEAN DEFAULT TRUE, created_at TIMESTAMP NOT NULL, updated_at TIMESTAMP NOT NULL);"
            }
          },
          {
            "sql": {
              "sql": "SET search_path TO core; INSERT INTO locations (id, name, code, type, parent_id, is_active, created_at, updated_at) VALUES ('c0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11', 'Canada', 'CA', 'COUNTRY', NULL, true, now(), now()); INSERT INTO locations (id, name, code, type, parent_id, is_active, created_at, updated_at) VALUES ('c0eebc99-9c0b-4ef8-bb6d-6bb9bd380a12', 'Ontario', 'ON', 'STATE', 'c0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11', true, now(), now()); INSERT INTO locations (id, name, code, type, parent_id, is_active, created_at, updated_at) VALUES ('c0eebc99-9c0b-4ef8-bb6d-6bb9bd380a13', 'Toronto', 'TOR', 'CITY', 'c0eebc99-9c0b-4ef8-bb6d-6bb9bd380a12', true, now(), now());"
            }
          }
        ]
      }
    },
    {
      "changeSet": {
        "id": "9-align-expertise-options-active-column",
        "author": "landgo",
        "changes": [
          {
            "sql": {
              "sql": "SET search_path TO core; ALTER TABLE expertise_options ADD COLUMN IF NOT EXISTS is_active BOOLEAN DEFAULT TRUE; UPDATE expertise_options SET is_active = COALESCE(active, TRUE) WHERE is_active IS NULL;"
            }
          }
        ]
      }
    }
  ]
}
